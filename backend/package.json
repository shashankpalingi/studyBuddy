{
  "name": "studybuddy-backend",
  "version": "1.0.0",
  "description": "StudyBuddy backend with Firebase and Supabase",
  "main": "index.js",
  "scripts": {
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:indexes": "firebase deploy --only firestore:indexes",
    "deploy:storage": "firebase deploy --only storage",
    "deploy:firebase": "firebase deploy --only firestore,storage",
    "setup:supabase": "echo 'Run the SQL commands in supabase/storage-config.sql in your Supabase dashboard'",
    "deploy:all": "npm run deploy:firebase && npm run setup:supabase"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "firebase-functions": "^4.5.0"
  },
  "devDependencies": {
    "firebase-tools": "^12.6.2"
  }
}
